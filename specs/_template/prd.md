---
feature: XX-feature-slug
title: 功能名稱
status: draft          # draft | definition | approved | shipped
owner:
depends_on: []         # 例：[04-rbac, 08-ticket-management]
design:                # 設計稿／原型連結（UI 不在 repo 內）
---

# Feature PRD — {{功能名稱}}

> **根基文件：** [`user-stories.md`](user-stories.md)（角色、情境、使用者目標——請先讀這份）
> **關聯文件：** `research/{{研究檔}}.md`、`../../_shared/user-journey.md`

<!--
  撰寫原則
  - User Story 是脊椎：UX Flow、功能需求、驗收標準都要對應得到某條 story。
  - 乾淨、解耦、自足：不纏繞其他 PRD，沒有上下文的人也能讀懂。
  - 不寫變更紀錄／版本號——歷史交給 Git，狀態寫在 front-matter。
  - 橫切、非 feature 的定義（如 RBAC）放 `_shared/`，引用而不重述。
  - 章節不編號。跨文件引用請寫章節名稱，不要寫 §1.3（編號會漂移）。

  章節順序固定如下。標「選填」者可省略，其餘必填；不要自創同義標題。
-->

## 核心概念
> 2–4 個 bullet，讓讀者在讀規格前先有掛勾。這份 PRD 的世界觀是什麼。

## 範圍與邊界
> 本文件負責什麼、**明確不負責什麼**。不負責的部分指向哪份文件。

## 使用者情境
> 誰、在什麼場景、遇到什麼痛點。2–4 句，沒有上下文的人也能懂。

## IDEAL Case（選填）
> 一切順利時的樣子，3–4 步敘事收在「結果」。回答「做對了長什麼樣」。

## User Story
> 引用 `user-stories.md`，只列本 PRD 覆蓋的那幾條，不重述全文。

- As a **{{角色}}**, I want **{{行為}}**, so that **{{價值}}**.

## UX Flow
> **給設計師對照製作介面**：使用者從進入到達成目標，介面上每一步互動。
> 寫「介面上發生什麼」，不寫技術實作。與 IDEAL Case 的差別：這裡逐步、可畫成畫面。

1. {{使用者進入 … 看到 …}}
2. {{使用者操作 … 系統回應 …}}
3. {{達成目標}}

## User Flow（選填）
> 跨模組、跨角色的旅程（例：Super Admin 建 Team → 邀請 → 對方接受）。
> 單一畫面內的互動請寫在 UX Flow，不要放這裡。

## 功能需求
> F1…Fn。每條寫系統行為與規格，不寫技術實作，並對應得到某條 story。

### F1. {{子功能}}
- {{系統行為 / 規格}}
- **權限：** {{哪些角色可執行}}（完整定義見 [`_shared/04-rbac.md`](../_shared/04-rbac.md)）

## 異常場景
> 表格化。編號會被其他文件引用，所以編號要穩定。

| 編號 | 場景 | 處理 |
|------|------|------|
| A1 | {{}} | {{}} |

## 成功指標
> **這功能上線後，怎麼判斷它成功了？** 可量測、與技術無關、有數字。
> 不是「能不能做到 X」（那是驗收標準），而是「做到之後改善了多少」。
> 例：派工從通報到承接的中位時間 ≤ 15 分鐘；重複工單比率 < 5%。

- {{指標}}：{{目前值 / 未知}} → {{目標值}}（{{量測方式}}）

## 驗收標準
> 可驗證、能判斷是/否，每條對應某條 story。這是 QA checklist。

- [ ] {{完成條件}}

## 開放問題
> 統一用這個標題（不要寫「待確認」「待拍板」）。每條寫清楚卡在哪、誰能拍板。

- [ ] **{{問題}}** — {{為何卡住}}；需 {{誰}} 決定。

## 相關 Feature
- [[XX-other-feature]] — {{關係}}
