# <FEATURE-ID> flow

This file describes sequence only. Product behavior is defined by [`spec.md`](./spec.md).

## <Flow name>

1. <Actor performs observable step>. (`<RULE-ID>`)
2. <System produces observable response>. (`<RULE-ID>`)
3. <Next actor or state transition>. (`<RULE-ID>`)

## Alternate and failure sequence

1. <Trigger and observable fallback>. (`<RULE-ID>`)
