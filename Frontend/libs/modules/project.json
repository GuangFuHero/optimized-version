{
  "name": "modules",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/modules/src",
  "projectType": "library",
  "tags": ["type:module"],
  "// targets": "to see all targets run: nx show project modules --web",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config libs/modules/vitest.config.ts",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
